function m(R1, R2, D1, D2) {
    
    var R = R2 - R1;
    var D = (D2 - D1) / (1000*60*60*24);
    var X = R/D;
    
    if (X >= 2000) {
        return;
    }
    
    if ((X == 15.0) || (X < 15.0)) {
        
        return (X * 15.0 * D);
        
    } else if ((X > 15.0) && ((X == 30.0) || (X < 30.0))) {
        
        return (((X - 15.0) * 20.0 * D) + (15.0 * 15.0 * D));
    
    } else if ((X > 30.0) && ((X == 50.0) || (X < 50.0))) {
        
        return (((X - 30.0) * 35.0 * D) + (15.0 * 20.0 * D) + (15.0 * 15.0 * D));
    
    } else if ((X > 50.0) && ((X == 70.0) || (X < 70.0))) {
        
        return (((X - 50.0) * 60.0 * D) + (15.0 * 20.0 * D) + (15.0 * 15.0 * D) + (20.0 * 35.0 * D));
    
    } else if ((X > 70.0) && ((X == 100.0) || (X < 100.0))) {
        
        return (((X - 70.0) * 75.0 * D) + (20.0 * 60.0 * D) + (15.0 * 20.0 * D) + (15.0 * 15.0 * D) + (20.0 * 35.0 * D));
    
    } else if ((X > 100.0) && ((X == 166.0) || (X < 166.0))) {
        
        return (((X - 100.0) * 150.0 * D) + (30.0 * 75.0 * D) + (20.0 * 60.0 * D) + (15.0 * 20.0 * D) + (15.0 * 15.0 * D) + (20.0 * 35.0 * D));
    
    } else if (X > 166.0) {
        
        return (((X - 166.0) * 200.0 * D) + (166.0 * 150.0 * D) + (30.0 * 75.0 * D) + (20.0 * 60.0 * D) + (15.0 * 20.0 * D) + (15.0 * 15.0 * D) + (20.0 * 35.0 * D));
    
    }

}

function t(R1, R2, D1, D2) {
    
    var R = R2 - R1;
    return (R * 130.0);
    
}

function r(R1, R2) {
    
    return R2 - R1;
    
}

function d(D1, D2) {
    
    return (D2 - D1) / (1000*60*60*24);
    
}